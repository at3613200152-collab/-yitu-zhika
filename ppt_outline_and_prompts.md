# 课程设计 PPT 大纲与生成提示词

> 用途：用此大纲直接生成 PPT，或把各页的「AI 提示词」喂给 Gamma / 讯飞智文 / 文心一格等 AI PPT 工具自动生成。
> 建议页数：15-20 页
> 风格：学术 + 工程并重，配色蓝白为主，避免花哨

---

## PPT 整体结构

| 章节 | 页数 | 内容 |
|---|---|---|
| 封面 | 1 | 标题 + 团队 + 日期 |
| 研究背景 | 2 | 论文背景 + 课题要求 |
| 算法流程 | 3 | 两阶段流程图 |
| 阶段一：NIR 生成 | 3 | 架构 + 数据 + 结果 |
| 阶段二：多任务网络 | 3 | 架构 + 数据 + 结果 |
| 对比实验 | 3 | 三模型对比 + 消融分析 |
| 演示应用 | 3 | 小程序截图 + 功能流程 |
| 总结与展望 | 2 | 完成情况 + 未来工作 |

---

## 逐页大纲与 AI 提示词

### P1 - 封面

**内容**：
- 标题：基于预测近红外光谱的多通道食物分类与卡路里估计
- 副标题：课程设计汇报 · 一图知卡
- 团队成员、指导教师、日期

**AI 提示词**：
```
生成一张学术风格的 PPT 封面页。标题："基于预测近红外光谱的多通道食物分类与卡路里估计"。
副标题："课程设计汇报 · 一图知卡"。
底部留团队成员、指导教师、2026-09 日期占位。
配色：深蓝 #1a3a6c 背景 + 白色文字 + 食物图标点缀。
风格：简洁、学术、工程感。
```

---

### P2 - 研究背景

**内容**：
- 论文：Multi-Spectral Food Classification and Caloric Estimation Using Predicted Images
- 问题：单 RGB 图像缺乏光谱信息，卡路里估计精度受限
- 思路：用生成网络预测 NIR 通道，拼接多通道特征提升精度
- 课题要求：复现论文 + 对比基线 + 演示应用

**AI 提示词**：
```
生成一页研究背景 PPT。左侧文字：论文标题、核心问题（RGB 缺光谱信息）、
关键思路（生成 NIR + 多通道拼接）。右侧配图：RGB 食物图 → 箭头 → NIR 预测图 → 多通道叠加。
配色蓝白，分点排版。
```

---

### P3 - 课题要求

**内容**：
- 阶段一：HSIFoodIngr-64 训练 RGB→NIR 生成器（U-Net/Pix2Pix），PSNR/SSIM 评估
- 阶段二：Nutrition5k 上 RGB+NIR 多通道 ResNet 多任务（分类 CE + 回归 L1/MAPE）
- 基线对比：CalorieCLIP 纯 RGB 方案，MAPE/RMSE 消融
- 演示应用：上传图像→展示 NIR + 类别 + 卡路里 + 基线对比
- 提交物：源代码 + 权重 + 报告

**AI 提示词**：
```
生成一页课题要求 PPT，用 4 个带图标的方块卡片展示：
① 阶段一 NIR 生成 ② 阶段二多任务网络 ③ 基线对比实验 ④ 演示应用。
每张卡片下方一行小字说明。配色：蓝底白字卡片。
```

---

### P4 - 算法整体流程

**内容**：
- 流程图：HSIFoodIngr-64 → U-Net/Pix2Pix → NIR 生成器 → Nutrition5k RGB+NIR 拼接 → ResNet 多任务 → 分类+卡路里+重量
- 同时分支：纯 RGB → CalorieCLIP 基线
- 两路对比：MAPE/RMSE

**AI 提示词**：
```
生成一页算法流程图 PPT。横向流程：
HSIFoodIngr-64 数据集 → U-Net+Pix2Pix 训练 → NIR 生成器（PSNR/SSIM 评估）
→ Nutrition5k RGB+预测NIR 拼接 → ResNet50 多任务网络 → 输出 分类+卡路里+重量。
下方分支：纯 RGB → CalorieCLIP 基线 → 对比 MAPE/RMSE。
用箭头连接，配色蓝橙对比。
```

---

### P5 - 阶段一：NIR 生成器架构

**内容**：
- 架构：U-Net 编码器-解码器 + PatchGAN 判别器（Pix2Pix 风格）
- 输入：RGB 3 通道，输出：NIR 1 通道
- 损失：L1 + 对抗损失 + 结构相似损失
- 关键修正：上采样通道 up3=768, up2=384, up1=192

**AI 提示词**：
```
生成一页 U-Net 架构图 PPT。展示编码器（下采样 4 层）→ 瓶颈层 →
解码器（上采样 4 层 + skip connection）。输入 RGB 3 通道，输出 NIR 1 通道。
右侧小框标注 PatchGAN 判别器。下方文字标注损失函数：L1 + Adversarial + SSIM。
配色：蓝色编码器、橙色解码器、灰色 skip 连接。
```

---

### P6 - 阶段一：训练数据与结果

**内容**：
- 数据：HSIFoodIngr-64 高光谱数据集
- 划分：训练/验证/测试
- 结果：PSNR **23.33 dB**，SSIM **0.812**
- 说明：生成的是相对强度预测图，非实测光谱

**AI 提示词**：
```
生成一页 NIR 生成器结果 PPT。左侧：数据集信息（HSIFoodIngr-64）。
右侧：两行数据卡片，PSNR 23.33 dB / SSIM 0.812，大号数字突出。
底部一行说明："生成的是相对强度预测图，非实测光谱"。
配色：深蓝背景 + 白色数据卡。
```

---

### P7 - 阶段二：多任务网络架构

**内容**：
- 骨干：ResNet50（ImageNet 预训练）
- 输入：RGB 3 通道 或 RGB+NIR 4 通道
- 三头：分类（11 类 softmax）+ 卡路里回归 + 重量回归
- 损失：Cross-Entropy + L1 + MAPE

**AI 提示词**：
```
生成一页 ResNet50 多任务网络架构 PPT。展示 ResNet50 backbone 分出三个头：
① 分类头（11 类 softmax + Cross-Entropy Loss）
② 卡路里回归头（L1 + MAPE Loss）
③ 重量回归头（L1 Loss）。
输入标注两种模式：RGB 3 通道 / RGB+NIR 4 通道。
配色：灰色 backbone + 蓝/绿/橙三色任务头。
```

---

### P8 - 阶段二：训练数据与配置

**内容**：
- 数据：Nutrition5k（本地俯拍子集）
- 划分：训练 2188 / 验证 567 / 测试 507
- 异常过滤：kcal [0,2000], grams [0,2000], kcal/g [0.1,9.0]
- 增强：RandAugment + MixUp(α=0.2) + Random Erasing(p=0.25)
- 种子：42

**AI 提示词**：
```
生成一页训练配置 PPT。用表格展示：数据集 Nutrition5k、
训练/验证/测试划分 2188/567/507、异常过滤阈值、数据增强策略、随机种子 42。
配色简洁，表格蓝白条纹。
```

---

### P9 - 阶段二：多任务结果

**内容**：
- 热量 MAE 56.55 kcal，RMSE 85.13，R² 0.839
- 重量 MAE 36.70 g
- 分类准确率 72.78%
- 散点图：预测 vs 真实（`results/course_report_plots/calorie_scatter_rgb_official.png`）

**AI 提示词**：
```
生成一页多任务网络结果 PPT。左侧四个指标卡片：热量 MAE 56.55、
RMSE 85.13、R² 0.839、重量 MAE 36.70。右侧放预测 vs 真实散点图占位。
配色：蓝色指标卡 + 灰色图表区。
```

---

### P10 - 对比实验：三模型对比表

**内容**：
- 表格：三模型 × 7 指标（MAE/RMSE/R²/MAPE/重量 MAE/准确率/SHA256）
- 三模型：纯 RGB / RGB+NIR / CalorieCLIP

**AI 提示词**：
```
生成一页三模型对比 PPT。用表格展示三行：
纯 RGB（MAE 56.55/RMSE 85.13/R² 0.839/MAPE 48.07%/重量 36.70/准确率 72.78%）、
RGB+NIR（56.21/84.90/0.840/51.02%/35.64/73.18%）、
CalorieCLIP（58.74/91.28/0.815/35.36%/不支持/不支持）。
最优值用绿色高亮。配色：蓝白条纹表格。
```

---

### P11 - 对比实验：消融分析

**内容**：
- RGB+NIR vs RGB：MAE 差 -0.34 kcal
- bootstrap 10000 次区间 [-3.091, +2.503] **跨过 0**
- 结论：**无显著增益**
- 诚实表述：不强行声称多模态优于 RGB

**AI 提示词**：
```
生成一页消融分析 PPT。用柱状图展示 RGB vs RGB+NIR 的 MAE 对比
（56.55 vs 56.21，差异极小）。叠加误差线标注 bootstrap 95% CI 跨过 0。
下方用红字结论："多模态未观察到显著提升"。配色：蓝橙对比柱。
```

---

### P12 - 对比实验：vs CalorieCLIP

**内容**：
- 我们 MAE 56.55 vs CalorieCLIP 58.74，优 2.19 kcal
- R² 0.839 vs 0.815
- 我们额外支持重量 + 分类，CalorieCLIP 只输出热量
- 优势：多任务、可追溯

**AI 提示词**：
```
生成一页 vs CalorieCLIP 对比 PPT。左右两列对比：
左列"我们的模型"：MAE 56.55、R² 0.839、支持重量+分类、可追溯。
右列"CalorieCLIP"：MAE 58.74、R² 0.815、仅热量、无分类。
我方优势用绿色勾，对方劣势用橙色叉。
```

---

### P13 - 演示应用：小程序架构

**内容**：
- 前端：微信小程序（4 tab：今天/饮食计划/历史/我的）
- 后端：Flask + ResNet50 + ImageNet 过滤
- 通信：HTTP + X-API-Key 认证
- 部署：localhost:8000（模拟器测试）

**AI 提示词**：
```
生成一页系统架构 PPT。左侧微信小程序图标 + 4 tab 示意。
中间 HTTP 箭头标注 X-API-Key。右侧 Flask 服务器图标 + 模块清单
（推理/过滤/反馈/营养师/采集）。配色：绿色微信 + 蓝色后端。
```

---

### P14 - 演示应用：识别流程

**内容**：
- 拍照/选图 → EXIF strip → ImageNet 过滤（非食物拦截）→ 主模型推理 → 结果展示
- 非食物：弹窗"未检测到食物"
- 食物：类别 + 卡路里(1 位小数) + 重量 + 三档反馈

**AI 提示词**：
```
生成一页识别流程 PPT。纵向流程图：
拍照 → EXIF 去除 → ImageNet 食物过滤 →（非食物：弹窗拒绝 / 食物：继续）
→ ResNet50 多任务推理 → 展示 类别+卡路里+重量+三档反馈。
每个节点用圆角矩形，箭头连接，非食物分支用红色。
```

---

### P15 - 演示应用：营养师模块

**内容**：
- TDEE 估算（Mifflin-St Jeor）
- 7 天食谱生成（5006 餐盘 + 过敏原过滤）
- 中文菜名映射（8 类 95 个菜名 + MD5 稳定映射）
- 免责声明"饮食参考，非医疗处方"

**AI 提示词**：
```
生成一页营养师模块 PPT。展示输入表单（身高/体重/年龄/性别/活动量/目标）
→ TDEE 计算 → 7 天食谱卡片（早/午/晚 × 7 天）。
右下角小字免责声明。配色：温暖橙色（健康食物感）。
```

---

### P16 - 演示应用：实测截图

**内容**：
- 食物识别截图（apple_pie 266.6 kcal）
- 非食物拦截截图（企鹅/动漫/蜘蛛侠 → 弹窗）
- 营养师食谱截图（中文菜名）
- API 测试结果表

**AI 提示词**：
```
生成一页实测展示 PPT。2×2 网格放四张截图占位：
左上"食物识别成功"、右上"非食物拦截"、左下"营养师食谱"、右下"API 测试结果"。
每张截图下方一行说明。配色：浅灰背景 + 白色截图框。
```

---

### P17 - 提交物清单

**内容**：
- ✅ 源代码仓库：GitHub `at3613200152-collab/-yitu-zhika`
- ✅ 模型权重：6 个（主模型 281 MB + NIR 生成器 190 MB + CalorieCLIP 448 MB 等）
- ✅ 演示应用：微信小程序 + Flask 后端
- ✅ 对比实验数据：三模型 audited 指标 + bootstrap 分析
- ⏳ 课程设计报告：待撰写（数据已备齐）

**AI 提示词**：
```
生成一页提交物清单 PPT。用 5 行带勾/fork 图标的清单：
源代码仓库 ✓、模型权重 ✓、演示应用 ✓、对比实验 ✓、课程报告 撰写中。
每行右侧标注简短说明。配色：绿色勾 + 橙色待办。
```

---

### P18 - 总结

**内容**：
- 完成了论文两阶段算法复现（NIR 生成 + 多任务网络）
- 完成了 CalorieCLIP 基线对比 + 消融实验
- 开发了完整的微信小程序演示应用
- 科学诚实：多模态未显著优于 RGB（如实表述）
- 工程完备：前后端 + 反馈系统 + 营养师 + 内测采集

**AI 提示词**：
```
生成一页总结 PPT。用 5 个带图标的要点：
① 论文两阶段复现完成 ② 基线对比+消融完成 ③ 微信小程序演示应用
④ 科学诚实表述 ⑤ 工程完备。
配色：深蓝背景 + 白色要点 + 绿色勾。
```

---

### P19 - 未来工作

**内容**：
- 多种子训练（当前单种子 42，需多种子区间）
- 分类头优化（解决"蔬菜"聚集）
- 真实 NIR 数据采集（当前生成的是预测图）
- 服务器部署 + 小程序发布上线
- 内测数据反馈迭代

**AI 提示词**：
```
生成一页未来工作 PPT。5 条展望：多种子训练、分类头优化、
真实 NIR 数据采集、部署上线、反馈迭代。
用箭头时间轴样式从左到右排列。配色：浅蓝渐变。
```

---

## 使用建议

1. **Gamma（推荐）**：把上述 19 页的「AI 提示词」逐个粘贴到 Gamma，一键生成整套 PPT。
2. **讯飞智文 / 文心一格**：把「内容」部分作为大纲导入，选择"学术报告"模板。
3. **手动制作**：按大纲在 PPT 软件中逐页制作，图表素材从 `results/course_report_plots/` 取。

## 图表素材位置

| 素材 | 路径 |
|---|---|
| 三模型散点图 | `yitu-zhika-code/results/course_report_plots/calorie_scatter_3models.png` |
| 主模型散点图 | `yitu-zhika-code/results/course_report_plots/calorie_scatter_rgb_official.png` |
| 消融 delta 直方图 | `yitu-zhika-code/results/course_report_plots/paired_delta_histogram.png` |
| 混淆矩阵（RGB） | `yitu-zhika-code/results/course_report_plots/confusion_matrix_rgb.png` |
| 混淆矩阵（NIR） | `yitu-zhika-code/results/course_report_plots/confusion_matrix_nir.png` |
| 训练曲线 | `yitu-zhika-code/results/phase2_eval_corrected/training_curves.png` |
| API 测试结果 | `yitu-zhika-code/results/api_smoke_recheck_result.json` |

# 减肥群体反馈与数据采集设计

日期：2026-09-07
范围：微信小程序在减肥群体（diet / weight-loss community）场景下的用户反馈、UI 交互、数据采集与模型再训练协议。
目标：把「让用户输入卡路里」这件反人性的事，改造成「一次点击即可，顺便产生高质量反馈信号」。

## 1. 设计原则

- 默认路径 = 一次点击；模型预测作为默认值，用户只决定「改 / 不改」
- 不强迫用户输入数字，不开放自由文本输入框
- 数据采集要让用户「看到自己贡献」，而不是「被监控」
- 减肥是敏感话题（身体形象、饮食障碍），不展示个体负向判断
- 数据生命周期可追溯：上传、匿名化、删除、模型再训练全部可解释

## 2. 反人性触点与对策

| 反人性行为 | 后果 | 对策 |
|---|---|---|
| 拍完照还要敲一遍 kcal | 3 日流失 80%+ | 预测值当默认；点「差不多」= 一次提交 |
| 自由文本输入「这是什么菜」 | 信噪比极低，不进训练 | 完全不开文本框；只点选 |
| 追问真实重量 / 油量 | 漏斗 2% 才能问 | 极少数主动展开才问 |
| 提示"今天超出 200 kcal" | 触发饮食障碍 | 永远不出个体负向判断；只出群体平均趋势 |
| 询问身高体重变化 | 暴露监控感 | 首次填一次后不再问；不参与训练 |

## 3. 交互形态（文字版）

```
┌──────────────────────────────────────┐
│  [照片缩略图]     预测：约 328 kcal  │
│                   重量：约 215 g     │
│                   类别：混合餐        │
│                                       │
│  ┌──────────────────────────────┐    │
│  │  ✓ 差不多（+0）             │ 高亮 │
│  │  偏少一点（−20%）            │      │
│  │  偏多一点（+20%）            │      │
│  │  我手动改…                   │      │
│  └──────────────────────────────┘    │
│                                       │
│  [提交]  [跳过这顿]                   │
└──────────────────────────────────────┘
```

三档点击覆盖 80% 场景；只有用户主动点「我手动改」才展开数字键盘（漏斗 2%）。

## 4. 数据字段（最少摩擦）

| 字段 | 必填 | 怎么收 | 用途 |
|---|---|---|---|
| 真实摄入是否如照片 | 必 | 上面 3 档 | 标签信号（最值钱） |
| 餐别（早/午/晚/加餐） | 隐式 | 时间戳判断 | 分布均衡 |
| 餐盘 UUID | 必 | 随机生成 | 跨餐纵向对照，匿名 |
| 真实重量 | 不收 | 仅在「我手动改」时收 | 校准绝对值 |
| 文字反馈 | 不收 | 不开输入框 | 信噪比太差 |
| 体重 / 目标 | 一次性 | 首次进入 | 周/月汇总，不入模型 |

不收：身高变化、心情、正餐零食分类、地理位置、识别个体身份。

## 5. 反馈质量分级

每条记录带 `user_correction_quality` 标签，加训时按权重处理：

| 等级 | 触发条件 | 训练权重 |
|---|---|---|
| `confirm_only` | 用户点「差不多」 | 低（噪声大） |
| `fine_adjust` | 用户点「偏少 / 偏多」 | 中 |
| `manual_typed` | 用户展开数字键盘并改值 | 高 |
| `skipped` | 用户点「跳过这顿」 | 不入训练 |

## 6. 反作弊与离群检测

- 连续 10 顿都按 `confirm_only` 且模型预测都在 ±10% 内 → 标记 `passive_participation`，训练时不进基线
- 单用户 24 小时内反馈超过 6 餐 → 标记 `overreport`，权重减半
- 单用户 7 天只点 `confirm_only` 不点 `manual_typed` → 群体基线对比，偏差 > 25% 则进入"复核名单"
- 不在产品层做封禁，只在训练时降权；用户感知不到

## 7. 模型再训练协议

新增训练源 `community_diet_v1`，独立 manifest、SHA256 绑定，绝不混入测试集。

```
S1 固定基线           (原 2188/567/507)
S2 加 128 餐盘侧视     (跨视角一致性训练)
S3 社区反馈 v1        (N 条, confirm_only 占多数)
S4 门控 / 因子化消融   (在 S1+S2+S3 上逐项加)
S5 多种子稳定性
```

硬约束：
- 社区反馈只进训练集；不进入验证 / 测试集
- 群体校准只回写到"我自己"（user-level bias head），不污染基线
- 每个训练版本 `protocol.json` 记录：参与人数、记录数、confirm_only 占比、时间跨度
- 任意一条用户撤回 → 立即重训对应版本（v1.1, v1.2...）

## 8. 合规与伦理

| 风险 | 应对 |
|---|---|
| 身体形象 / 饮食障碍触发 | 群内固定声明"非医疗建议"；不展示个体负向判断；只出群体平均趋势 |
| 减肥药 / 极端节食 | 不主动询问；结果页固定"如有健康问题请咨询医生" |
| 未成年识别 | 微信侧已限制；服务端再过一次 |
| 数据出域 | 只自托管；图片 EXIF 在上传前 strip；6 个月后匿名化（去掉 user_id，仅留 dish 维度） |
| 退出 | 一键导出 / 删除；模型权重不含个人数据，可证明 |
| 群体治理 | 群主可见群体趋势；不可见个体详情 |

## 9. 留存引擎：让贡献被看见

每周一自动发群消息模板：

```
[一图知卡 · 上周回顾]
参与成员 32 人 · 上传 187 餐 · 跳过率 12%
您帮助模型把 MAPE 从 48% 降到了 44%（Nutrition5k 冻结测试集）
本周群体平均摄入：1,820 kcal/天（仅供参考，非医疗建议）
```

- 数字真实、来源 manifest 哈希可查
- 不点名任何个体
- 「帮助模型」比任何积分都管用
- 这是 6 个月后仍能保留这群人的真正原因

## 10. 与三方向消融的衔接

可学习 NIR 门控、质量密度因子化、跨视角一致性这三条当前都是离线实验。
社区反馈数据上线后：

1. 不参与这三个实验的基线构造
2. 仅作为「真实场景微调」独立通道
3. 训练时分别跑：
   - `v2_baseline` （原 3262 + 128 侧视）
   - `v2_baseline + community_v1`（确认效果来自社区反馈）
   - `v2_baseline + community_v1 + 门控 / 因子化 / 多视角`（组合消融）
4. 测试集永远不变，永远是 Nutrition5k 冻结 507

## 11. 实施清单

后端：
- [ ] `app/server.py` 新增 `POST /api/feedback`
- [ ] 入参：`{dish_uuid, image_hash, predicted_cal, predicted_mass, user_grade, ts, app_version}`
- [ ] 出参：`{ok, contribution_id}`
- [ ] SQLite 表 `feedback_v1(id, dish_uuid, image_hash, predicted_cal, predicted_mass, user_grade, quality, ts, app_version, user_hash)`
- [ ] 6 个月匿名化任务：去掉 `user_hash` 字段，仅保留 `dish_uuid`

训练：
- [ ] `src/training/train_meal_with_feedback.py`
- [ ] 加 `user_level_bias_head`（轻量 MLP，只在有 feedback 的样本上训）
- [ ] 训练时显式记录 `community_diet_v1` manifest 与 SHA256
- [ ] 多种子（≥3）+ 配对 bootstrap 报告

小程序：
- [ ] 上传前 strip EXIF（`canvas` 重绘一次）
- [ ] 结果页加三档按钮 + 「我手动改」折叠
- [ ] 「跳过这顿」按钮，零成本退出
- [ ] 首次进入一次性问卷：目标方向（增肌/减脂/维持），不参与训练
- [ ] 「我的贡献」入口：上传餐数 / 跳过率 / 模型提升百分比
- [ ] 一键导出 + 一键删除

群运营：
- [ ] 每周一自动发群回顾（人数、餐数、MAPE）
- [ ] 不点名任何个体
- [ ] 固定"非医疗建议"声明

## 12. 验收门禁

- 测试集不变（Nutrition5k 冻结 507）
- 每个版本 `protocol.json` 写明数据来源、hash、参与人数、跳过率
- 用户撤回能在 24 小时内让对应样本从训练集移除并重训
- 训练指标 + 群体平均摄入两套数字分别报，互不冒充
- 三档点击率与手动改率分别统计；用户级偏差 head 与基线模型分别存
- 任何对未参与用户的预测不能引用其个人数据

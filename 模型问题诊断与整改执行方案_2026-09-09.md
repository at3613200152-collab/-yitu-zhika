# 一图知卡：模型诊断与整改执行方案

日期：2026-09-09
工作台：`C:/Users/user/Desktop/新建文件夹`
最终产品：微信小程序。网页 Demo 仅用于模型展示、诊断和对照。

## 一、结论

当前问题不是单一 UI 缺陷，也不能靠多训练几轮一起解决。已经确认：

1. 粗标签生成存在子串误匹配，且类别分布严重不均衡。
2. 两个当前主模型明显偏向 vegetable；但不是所有输入都固定输出蔬菜。
3. 当前主模型实际只有热量、重量两个回归输出，接口和结果页也未接入三大营养素。
4. 主服务把 NIR 模型的预测标记为 RGB 模型版本，溯源不一致。
5. 新增 128 餐盘在扩展清单中，但本次检查的两个主模型还使用旧清单。
6. 消融训练的续跑完成状态校验测试失败，应在长训前修复。
7. 队友权重不能直接替换：一份是 16 类，另一份尚未通过安全加载核验。

建议顺序：**模型契约 → 续训可靠性 → 标签审核 → 五目标数据与训练 → 128 餐盘增量对照 → NIR/CLIP/多视角消融 → 小程序验收。**

本次使用 diagnose 的复现、核查和验证流程。只进行了 CPU 离线推理、文件核对、已有预测统计和测试；没有覆盖权重、启动训练或调用收费第三方模型。

## 二、可复查的证据与边界

- [诊断脚本](C:/Users/user/Desktop/新建文件夹/yitu-zhika-code/scripts/audit_current_inference.py)
- [机器可读诊断证据](C:/Users/user/Desktop/新建文件夹/artifacts/model-audit-2026-09-09/evidence.json)

脚本以 weights_only=True 安全读取权重，使用 CPU、eval、inference_mode 和当前主推理相同的预处理。输出文件已存在时拒绝覆盖。

本次核查两个当前权重 SHA、输入通道、输出头和 protocol；从已保存的 507 条预测重算分类指标，并对六张本地图片重新进行 RGB/NIR 推理。

边界：

- 六张图片的文件名不是经审核的真值，没有称重和营养真值，不能据此计算热量误差或整体准确率。
- 507 条分类统计来自已保存预测，不是本次完整重跑测试集。
- 六图探针绕过 HTTP 食物过滤器，证明主模型行为，不等于手机端端到端测试。
- 服务运行进程是否使用磁盘最新代码，仍要在明确重启后核验。
- 工作台存在队友并行修改。本报告是检查时的快照，不覆盖他们的修改。

## 三、权重清单：哪些能用，哪些不能直接套用

以下路径均相对于工作台。

| 路径/版本 | 实际核查 | 处理方式 |
|---|---|---|
| yitu-zhika-code/checkpoints/meal_rgb_official_v1/best.pt | 3 通道、11 类、2 回归输出；SHA 与指标一致 | 保留内部 RGB 对照 |
| yitu-zhika-code/checkpoints/meal_nir_official_v1/best.pt | 4 通道、11 类、2 回归输出；SHA 与指标一致 | 保留 NIR 对照，版本必须标对 |
| yitu-zhika-code/checkpoints/multitask/best_model.pt | sidecar 声明历史顺序 mass、calories，classification_valid=false | 不启用其分类，不按新顺序硬读 |
| Desktop/checkpoints_rgb_only/multitask/best_model.pt | 安全加载成功；epoch=66；16 类、2 回归输出；顶层缺少目标名称/类别映射 | 补齐原训练配置、映射和数据来源，再评估 |
| food_calorie_estimation/checkpoints/multitask/best.pth | 文件存在，约 308.8 MiB；安全加载失败 | 结构和性能尚未核验，不能直接套用 |
| yitu-zhika-code/checkpoints/hsi_unet_v3_real_nir/final_model.pth | 文件存在，当前主管线没有自动选用它 | 文件名不是波段、泛化或性能证据 |

已核对的 SHA256：

- RGB：`3f189a439fca3b79ecd820ae68605bcbfe61679def14f8dfa548c34261c89cf0`
- NIR：`3a13788acf640fe807b0bd60fc5fb0f35251cbd2e994f2451fa903ff6ffae950`

安全加载失败不意味着权重一定损坏或恶意。请队友在可信原训练环境导出纯 state_dict 和独立 JSON 契约，提供架构、类别映射、目标顺序/单位、归一化、数据划分、代码版本及哈希。不要为方便直接设 weights_only=False。

## 四、分类为什么偏向蔬菜

### 4.1 已排除“结果页统一写死蔬菜”

结果页读取 record.result.category_name，空值回退为“未知类别”；主推理按 logits 最大项和 manifest 映射类别。当前已检查的 RGB/NIR 训练、主推理与小程序修改功能使用相同 11 类顺序，未发现这条链路整体索引错位。这个结论不能套用到队友 16 类模型。

实际探针结果如下，百分数仅为未校准模型分数：

| 本地文件 | RGB | RGB＋NIR |
|---|---|---|
| test_apple_pie.jpg | vegetable 99.72% | vegetable 98.94% |
| test_baby_back_ribs.jpg | other 42.49% | other 33.18% |
| test_bibimbap.jpg | vegetable 95.83% | vegetable 95.25% |
| test_bread_pudding.jpg | vegetable 40.32% | egg 45.74% |
| test_caesar_salad.jpg | vegetable 95.84% | vegetable 98.86% |
| user_mixed_meal.png | vegetable 52.09% | vegetable 61.82% |

两个模型都偏向蔬菜，不是只由 NIR 引起。服务还把 category_prob 四舍五入到一位小数，0.989 会变为 1.0，容易让人误以为百分之百正确。

### 4.2 确认存在错误的标签规则

[build_categories.py](C:/Users/user/Desktop/新建文件夹/yitu-zhika-code/src/data/build_categories.py) 使用按优先级执行的 `if kw in name_lower` 子串匹配。实际最小探针：

| 输入 | 当前输出 | 错误风险 |
|---|---|---|
| apple pie | vegetable | apple 先于 pie 命中 |
| eggplant | egg | egg 命中 eggplant 的一部分 |
| graham cracker | meat | ham 命中 graham 的一部分 |
| cream of mushroom soup | dairy | cream 先于 soup 命中 |
| white rice | grain | 对照项 |
| grilled chicken | meat | 对照项 |

这证明规则缺陷，不代表这些字符串全部出现在实际训练数据中。实际影响样本数仍须在食材表和餐盘上审计。

此外，vegetable 规则包含 apple、banana 等水果和部分调味食材，界面却只显示“蔬菜”。餐盘标签又按食材克重累计，最大类占比达到 40% 就选它，否则 mixed。因此这套标签实际是“主要食材重量组”，不是精细菜名或逐项食材识别。仅在 UI 增加解释不算修复分类。

### 4.3 数据和评估偏置

| 类别 | 训练餐盘数 |
|---|---:|
| vegetable | 1184 |
| meat | 244 |
| egg | 203 |
| other | 182 |
| mixed | 146 |
| seafood | 86 |
| dairy | 85 |
| grain | 48 |
| sauce_condiment | 6 |
| dessert | 4 |
| soup_stew | 0 |

共 2188 餐盘，蔬菜约占 54.1%；验证集没有 dessert 和 soup_stew。重复采样无法创造零样本类别知识，也不能让四张甜点图覆盖真实变化。

| 指标（现存 507 条测试预测） | RGB | RGB＋NIR |
|---|---:|---:|
| 粗类别 accuracy | 72.78% | 73.18% |
| 有测试支持类别的 Macro-F1 | 51.84% | 50.37% |
| 有测试支持类别的平均 recall | 50.80% | 47.64% |
| 预测为 vegetable 的数量 | 310 | 320 |
| 测试标签为 vegetable 的数量 | 276 | 276 |
| 预测为 dessert 的数量 | 0 | 0 |

Macro-F1/平均 recall 此处对测试中有样本的 9 类求平均。sauce_condiment、soup_stew 测试支持数为 0，不能宣称已验证它们的能力。后续应同时给出全标签集合与有支持类别口径，并公开支持数。

尚待验证：手机域与食堂俯拍域差异、背景捷径、NIR 跨域影响、真实标签误标比例、食物过滤器误拒率。这些不能写成已证实的唯一原因。

## 五、三大营养素缺失的数据链路

### 5.1 不是只漏了三个文本框

1. [train_meal_official.py](C:/Users/user/Desktop/新建文件夹/yitu-zhika-code/src/training/train_meal_official.py) 指定 TARGET_NAMES 为 calories、mass，并显式构建两输出网络。
2. 两个实际 checkpoint 的 network.regressor.3.weight 均为 [2,512]。
3. [experiment_pipeline.py](C:/Users/user/Desktop/新建文件夹/yitu-zhika-code/app/experiment_pipeline.py) 未生成 protein/carb/fat 字段。
4. [contracts.py](C:/Users/user/Desktop/新建文件夹/yitu-zhika-code/app/contracts.py) 没有宏量营养素契约，而且主服务未完整使用该契约进行序列化。
5. [结果页模板](C:/Users/user/Desktop/新建文件夹/miniprogram/pages/result/result.wxml) 没有蛋白质、碳水和脂肪展示区。

ResNet 源文件的注释和默认值支持五输出，不代表已训练好的两输出权重具有五项能力。不能通过把 2 改成 5、非严格加载或随机初始化三个头，就宣称营养素可用。

### 5.2 可靠标签已经存在

`data/Nutrition5k/dishes_verified.csv` 包含 total_calories、total_mass、total_fat、total_carb、total_protein。本次已查看表头及数据行。Nutrition5k 官方也提供这些营养标签与划分。[官方数据说明](https://github.com/google-research-datasets/Nutrition5k)

已有历史审计发现旧 dishes.csv 的热量/重量整列互换；当前官方 v1 已使用修正来源。不要直接使用旧 CSV 的列位置，也不要把历史列错位直接归罪于当前官方模型。

当前两目标模型的宏量营养素应显示“暂不支持/待补充”，值为 null；真实零值才显示 0。用户确认、用户估计、称重、数据库匹配和模型预测要分开存储来源。用油量、生熟状态和烹饪方式未知时保留未知。

## 六、必须先修复的工程问题

### 6.1 模型版本错标

[inference_service.py](C:/Users/user/Desktop/新建文件夹/yitu-zhika-code/app/inference_service.py) 声明 ONLINE_MODEL_VERSION=meal_rgb_official_v1，SHA 前缀也来自 RGB；但创建 ExperimentPipeline 后，其 calories/weight/category 主结果实际来自 NIR，RGB 仅作为对照字段返回。

因此只修改 ONLINE_MODEL_PATH 并不会实际切换推理分支。预测结果和收集记录的来源会被错误标注。

### 6.2 续训状态测试失败

现有 tests/test_ablation_resume.py 构造 epoch=1、计划30轮、patience=8，却标记 training_complete=true；当前 resume_state 未抛出错误，测试失败。这可能让长训错误地认为已经完成。新一轮无人值守训练前必须先修复。

## 七、整改执行任务

以下是待执行计划，不代表已经实现；先完成前置任务，再训练。除第八节明确列出的命令外，不把待开发脚本当成现有命令。

### P0-A：统一模型选择、输出契约和营养素状态

文件：app/inference_service.py、experiment_pipeline.py、contracts.py、model_registry.py；小程序 utils/record.js、pages/result/。

- [ ] 把实际模型对象、版本、SHA、target_names、label_schema、预处理绑定为一个经验证的模型描述。
- [ ] 主结果明确选择 RGB 或 NIR，版本来自同一个对象；Demo 对照输出独立命名。
- [ ] /model-info 返回实际加载对象与能力，不写死未使用模型。
- [ ] 契约新增可空 protein_g、carbohydrate_g、fat_g，附支持状态、单位和逐字段来源。
- [ ] 规划 calories/weight 到带单位字段的兼容迁移，避免旧小程序和历史记录失效。
- [ ] 详情、历史和收集字段一致；缺失显示未知，真实零值显示 0。
- [ ] 模型分数不显示为准确率，不用粗类别置信度低就断言“不是食物”。

验收：用固定 mock 输出验证“数值—版本—SHA”对应；旧记录可读；两目标模型不产生伪造宏量营养值；类别映射不一致时明确拒绝。

### P0-B：修复断点续训可靠性

文件：src/training/train_meal_ablation.py、tests/test_ablation_resume.py。

- [ ] 校验 config、epoch、history 连续性、best_epoch 与 last/best 对应关系。
- [ ] 完成状态必须可由达到预算或有效早停推出，不能只信任一个布尔值。
- [ ] 补合法早停、达到预算、异常提前完成、配置改变和半写文件测试。
- [ ] 做一次小预算“保存→中断→恢复”，验证优化器、随机状态和轮次正确衔接。
- [ ] 每次新实验独立目录，原权重与审计文件只读保留。

验收：当前失败测试通过；异常完成被拒绝；合法恢复可继续，不重复或跳过 epoch。

### P1-A：修标签，并针对缺口补数据

文件：src/data/build_categories.py、verify_nutrition5k_metadata.py、meal_manifest.py、meal_expanded_manifest.py；新增版本化标签审计产物。

- [ ] 输出“实际食材→命中词→粗类别→影响餐盘”的审计表，先确认错误规模。
- [ ] 用实体词典、词边界和长短语优先替代任意子串；保留命中原因及人工覆盖机制。
- [ ] 为四个规则错例添加测试；测试字符串不直接当作真实餐盘训练标签。
- [ ] 分清食材组、菜名、烹饪方式。水果/蔬菜/甜点的语义需要一致，改类别集合时升级 label_schema。
- [ ] 优先审核高分错例、少数类和中式混合餐；再补充获准使用的手机照片，记录来源许可并去重。
- [ ] 原冻结划分不原地改写；新增独立手机场景验证集，同盘不同角度不跨集合。
- [ ] 对无验证支持的类别补独立验证样本。修订旧测试标签时保留版本差异及新旧口径，不借修标签美化旧成绩。

训练建议：先尝试仅分类损失加权，例如训练频率平方根倒数并封顶；参数用验证集选择。CrossEntropyLoss 支持逐类权重。[PyTorch 文档](https://docs.pytorch.org/docs/stable/generated/torch.nn.CrossEntropyLoss.html)

不要首次就叠加强过采样和极端权重；共同采样会改变营养回归分布，优先保持回归按餐盘等权，将分类加权作为单独对照。

验收：样本影响可追溯；支持数公开；逐类 precision/recall/F1 改善；不依赖伪标签或数据泄漏。

### P1-B：补齐五目标数据和真实五目标训练

- [ ] 从 dishes_verified.csv 按字段名构建 calories、mass、protein、carb、fat；显式单位与缺失 mask。
- [ ] train-only 统计归一化，缺失维度不参与损失；不能把未知补0。
- [ ] 五输出结构真实训练；从旧骨干迁移时明确哪些参数复用、哪些头新初始化。
- [ ] 比较分目标标准化 L1/Huber；避免重量量纲压过蛋白质等目标。
- [ ] 对物理非负输出做可训练参数化，并保留原线性头对照；不事后裁零美化旧指标。
- [ ] 4P+4C+9F 只作近似一致性检查或经验证的弱约束，不能代替标签。
- [ ] API、结果页、历史记录接入全部字段及“估算/未知”状态。

验收：五个目标分别有来源、单位和独立验证指标；旧权重不能假装支持五项；无三大营养素的旧记录不会报错。

### P1-C：让新增128餐盘进入下一轮，而不是只存在磁盘上

本次核对：

- 旧 manifest：f1e293b826c98e517c3d1179871ec0a10d8ee471652b5d2e6f89f9ed1c755daa。
- 扩展 manifest：32f4b53665c950fb86c54f842d3d134aaabfcaa5302e15ad7ed82e6898395afb。
- 扩展后 train=2316、val=567、test=507；128 个新增餐盘全为 train。
- 原扩展审计记录新增384视图，holdout行未变，未补入烹饪标签。
- 本次检查的两个主模型仍用旧 manifest；不能据此断言所有队友实验都没用过扩展数据。

- [ ] 新五目标 manifest 保留原 split，并为新增餐盘关联可靠营养字段。
- [ ] 按餐盘采样，三个角度不当三个独立餐盘加权。
- [ ] 同模型/预算对比旧数据与扩展数据，独立测“补数据”的收益。
- [ ] 俯拍评测保留；侧视泛化另建独立评测集。
- [ ] 生熟重、称重方法、烹饪方式和油量只记录真实来源，未知保留未知。

### P2：逐项消融，而不是一次把所有改动叠上

| 对照 | 主要变化 | 要回答的问题 |
|---|---|---|
| A/B | 相同旧配方，旧数据 vs 加128餐盘 | 补数据有没有帮助 |
| C | B加标签纠偏/分类权重 | 类别纠偏是否有效 |
| D | C加五目标回归 | 三大营养素可用吗，热量有无退化 |
| E1/E2 | 相同D配方，RGB vs RGB+预测NIR | NIR本身收益 |
| F | E2加NIR门控 | 是否能抑制不可靠辅助通道 |
| G | 同一基线替换重量×密度头 | 热量分解的泛化收益 |
| H0/H1 | 相同双视角监督，无/有一致性损失 | 多监督与一致性的作用分离 |
| I | 同一基线加冻结CLIP辅助 | 类别收益及回归代价 |
| J | 仅组合已验证有效模块 | 组合是否仍然有效 |

这些是实验设计，不是全部已有现成 CLI。先补实现与测试，先小规模 smoke，再预先固定多种子预算，避免盲跑所有排列组合。

CLIP注意：

- 当前 CLIPFoodClassifier 文件存在，但主 ExperimentPipeline 分类仍取 NIR logits，没有调用它。
- CalorieCLIP 外部热量基线与 CLIP 食物分类器不同，不能混称已经接入。
- 先试冻结语义特征/候选类别，统一词表和提示词，再比训练分类头。
- softmax相似度不是食材克重比例，不能据此加权成精确营养真值。缺失/失败要显式未知。
- CLIP支持图文迁移，不保证本项目菜名、重量和热量精度。[原始论文](https://proceedings.mlr.press/v139/radford21a.html)

NIR注意：由同一RGB确定性生成的NIR不是新增实测传感信息；可能提供有用表示或先验，但必须用对照证明。不能凭轮廓可见证明光谱准确，不能保证数据补齐后必达论文增益。门控值也不是校准后的可靠性概率。

## 八、现在可执行的诊断命令

### 8.1 复跑本次模型核查

```powershell
Set-Location 'C:\Users\user\Desktop\新建文件夹\yitu-zhika-code'
$taskStamp = Get-Date -Format 'yyyyMMdd-HHmmss'
$taskEvidence = "C:\Users\user\Desktop\新建文件夹\artifacts\model-audit-$taskStamp\evidence.json"
& 'C:\Users\user\miniconda3\envs\yitu\python.exe' '.\scripts\audit_current_inference.py' --output $taskEvidence
if ($LASTEXITCODE -ne 0) { throw '诊断未完成，请检查错误，不要接着训练' }
```

### 8.2 续跑测试（目前失败，修复后必须通过）

```powershell
Set-Location 'C:\Users\user\Desktop\新建文件夹\yitu-zhika-code'
& 'C:\Users\user\miniconda3\envs\yitu\python.exe' -m unittest tests.test_ablation_resume -v
if ($LASTEXITCODE -ne 0) { throw '续跑验收失败，暂停长训' }
```

### 8.3 小程序UI回归

```powershell
node --test 'C:\Users\user\Desktop\新建文件夹\miniprogram\tests\ui-regression.test.cjs'
```

本轮前半段实际结果：4项回归通过；320/375/430宽度下浏览器近似布局检查通过；微信自带编译器检查8个WXML、9个WXSS通过；页面JS语法通过。不是手机真机验收。

## 九、UI修改交接

此前已修改问卷按钮居中、性别/目标溢出、食物字段标签/单位、足迹循环别名、忌口选中态、跳过问卷导航、字体与相机/日历/个人图标；纠正了“确认后模型立即变准”等过强文案。后端地址、API Key和用户历史未因这批UI修改而改变。

待真机验收：

- [ ] 重新编译并扫码当前版本；无需清空用户数据。
- [ ] 问卷长文案、大字体、小屏无裁字；选中/取消状态清晰。
- [ ] 足迹在无记录和有记录时都正常显示。
- [ ] 饮食计划输入、picker和添加按钮可点且不超屏。
- [ ] 确认/修改、历史页继续检查同类原生按钮尺寸问题。
- [ ] 接入营养素后对缺失、零值、旧记录、服务失败分别截图验收。

本报告中的分类/营养素整改仍是待办，不能标记为已上线。

## 十、训练和发布验收门槛

- 模型选型用验证集，冻结后再做507餐盘最终比较；不能反复按测试成绩挑模型。
- 同盘多视图聚类计算区间，不当成独立样本。分类逐类支持数、Macro-F1、balanced accuracy与混淆矩阵必须齐全。
- 回归报告五目标MAE/RMSE和低值分层；MAPE与相对MAE分开，不删低热量样本提高成绩。
- 当前保存的RGB/NIR热量MAE分别56.55/56.21kcal，MAPE为48.07%/51.02%；单种子小幅MAE差异不能代表稳定改进。
- 比较保持精度一致；原正式评估BF16，本次CPU探针FP32。
- 训练前登记接受阈值和预算；不根据测试结果事后改规则。
- 异常原始输出保留审计；用户界面返回待确认/估算失败，不把负数当正常营养参考。
- 用户估计、确认和称重区别存储；只有独立授权、审核并去标识化的记录进入候选训练池。
- 初期面向减重群体定位为识别/记录可用性内测，不宣传精准减重或营养诊断。
- 项目较完整后再按用户要求推GitHub；提交代码、文档和汇总指标，不直接推真实照片、密钥和身份信息。

## 十一、与队友已有文档的差异

[模型吸收清单](C:/Users/user/Desktop/新建文件夹/yitu-zhika-code/docs/模型吸收与对比评估_2026-09-09.md) 中“修改ONLINE_MODEL_PATH即可切换”的步骤与当前实现不符，应先完成P0-A；用测试MAE筛选候选也应改为验证集选型、测试最终评估。

[复现差距分析](C:/Users/user/Desktop/新建文件夹/yitu-zhika-code/docs/复现差距分析与合规复现方案_2026-09-09.md) 对“独立光谱信息”和补齐数据后的收益前提表述过强。跨域和数据规模是合理待验证因素，不是已证实唯一原因或增益保证。本轮未改写队友文档，保留供讨论。

最终交付顺序：先让预测来源可信、标签可解释、数据与训练可复跑，再谈泛化和创新；完成后接入微信小程序，而不是停留在网页Demo。


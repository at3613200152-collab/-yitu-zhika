# 当前堵点解决与验收执行方案

日期：2026-09-10  
项目：一图知卡——微信小程序食物识别与营养估算  
工作台：`C:\Users\user\Desktop\新建文件夹`  
模型与后端：`yitu-zhika-code/`；小程序：`miniprogram/`

> 本文件是待执行方案，不是完成报告。依据用户提供的堵点截图、最新本地报告和部分代码核对编写；不代表已经完成新一轮训练、真机测试或独立复算全部指标。执行前重新检查当前代码与产物，防止与队友正在进行的工作冲突。

## 1. 目标和处理顺序

最终交付是微信小程序，不是网页 Demo。近期目标是：获得可信的实验对照、可追溯的五项营养预测，以及可以进行受控内测的完整使用流程。

依次推进：

1. 保存协作现场、冻结实验口径，排除权重与评估链路问题。
2. 完成小程序拍照、识别、确认、保存与历史记录真机闭环。
3. 建立未参与训练的多角度评测集，查清角度与份量的影响。
4. 在干净 RGB 基线上，分别处理负预测、少数类别和视角问题。
5. 再评估 NIR 门控、CLIP、重量×密度等模块，按结果决定是否上线。

不以下载完 650 GB 数据为所有工作的前提；不同时修改多个因素后将效果归因于其中一个。

## 2. 先修正现有报告中的判断

| 当前表述 | 应修正为 | 后续证据要求 |
|---|---|---|
| CLIP 出现 `No pretrained weights loaded`，所以基线不可靠 | 该警告可能来自先创建空结构。当前训练代码随后严格加载本地权重，仍需核对对应运行的最终加载链路 | 权重 SHA、加载键检查、训练来源与固定图片复算 |
| 侧拍热量误差 94%、重量误差 113%，说明全是角度问题 | 历史 `v1` 在另一批侧视餐盘上的风险信号；角度、餐盘内容、份量及历史选模问题存在混淆 | 同餐盘配对多角度、独立留出、统一模型与预处理 |
| 94%/113% 是 MAPE | 现有报告使用 MAE/标签均值，不是逐样本 MAPE | 重新核对分子分母来自相同样本与权重；分别报告两个指标 |
| 已确定运行噪声为 ±1 kcal | 两次运行观察到约 1 kcal 差异，尚未估计稳定分布，也未排除代码、初始化、采样等差异 | 完整运行配置与同配置重复实验 |
| 水果标签导致热量退化约 1.9 kcal | 当前旧组混用了训练数据不同、重复 seed 和历史存在缺陷的运行，不能直接作因果结论 | 旧/新标签组在同数据、同代码、同种子下配对实验 |
| 测试指标不受影响，所以选模问题无碍 | 某个权重的测试指标可以计算正确，但产生该权重的选模过程仍可能不正确 | 重算保存检查点的验证指标，或重新训练 |
| 相邻帧误差低，证明泛化有效 | 同一训练餐盘的近邻视频帧只能作一致性诊断，不是未见餐盘泛化证据 | 按物理餐盘/采集序列隔离的新评测数据 |

补充：像素差较小不能单独证明模型“记忆”，也不能把某个像素差阈值当成通用去重标准。决定评测是否独立的首要依据是餐盘与采集来源，而不是图像差值。

## 3. P0：保存现场与冻结实验口径

### 3.1 协作保护

- [ ] 查看 Git 状态，列出队友未提交改动、当前修改、未跟踪文件及可能重叠的代码片段。
- [ ] 在明确归属后保存本地快照或补丁；先检查是否包含密钥、用户照片或训练数据，不自动全部加入 Git。
- [ ] 不执行强制回退，不覆盖队友文件，不删除旧权重或旧清单。
- [ ] 对冲突部分逐项合并；暂时无法协调的修改保留补丁，不阻塞无关工作。
- [ ] 本阶段不自动推送 GitHub，不终止队友训练进程。

### 3.2 每个正式运行必须记录

运行 tag、seed、代码提交或源码哈希、数据清单哈希、标签版本、类别映射、五个目标名称与单位、缺失标签 mask、预处理、初始化权重哈希、优化器、学习率、训练预算、早停规则、验证选模指标、精度设置、采样规则、软件/设备版本。

要求：

- 同名输出目录非空时拒绝覆盖；续跑必须显式开启并检查兼容性。
- 保存 optimizer、scheduler（如有）、随机状态和训练进度；区分推理权重与续跑状态。
- 使用临时文件加原子替换写检查点。
- 验证连续训练与中断恢复的行为，不能只检查“成功退出”。
- 新版脚本与旧产物不一致时，不直接将旧产物视为新版训练结果。

### 3.3 修复选模口径

先定位究竟哪个脚本、哪一版本、哪几次运行受影响。若只影响验证聚合且保存了全部候选检查点，可按预先固定的正确规则重算验证指标并选模；若检查点不全，或缺陷影响训练损失/梯度，必须使用新 tag 重训。

历史结果保留并标注限制，不伪装成修复后的实验。测试集不参与选模；如果历史上已经反复用测试成绩决定方案，应披露，并以新的独立留出集作最终确认。

验收：每条正式结果可以追溯到唯一代码、数据、标签与权重；验证选模正确；逐样本预测可以独立复算指标。

## 4. P0：CLIP 对照核验，不凭警告删基线

已查到 `src/training/train_calorieclip_official.py` 先创建 `pretrained=None` 的结构，随后通过 safetensors 与 `strict=True` 加载本地通用权重；已有 `cpu_validation.json` 记录严格加载通过。现有三个指标单元测试通过，但这些证据不替代当前训练产物的完整复验。

执行：

1. 分清 CalorieCLIP 热量基线、CLIP 辅助分类实验、线上对照加载，分别核查，不混为一个模型。
2. 从目标运行配置解析真实权重路径，计算 SHA，与该运行记录核对。
3. 检查加载后的缺失键/多余键与结构兼容性。权重缺失或不匹配必须报错，不允许静默随机推理。
4. 核对训练初始化确实加载了通用 CLIP 权重，且未使用训练集来源不明的食物微调权重污染主对照。
5. 固定少量图片，对最终检查点重复推理；预处理、设备精度与输出容差须记录。
6. 用保存预测重新计算热量 MAE、RMSE、MAPE，并核对指标文件与检查点哈希。

决策：证据完整则保留，称为“CalorieCLIP 结构的本地受控再训练”；证据不足则标为待核实，不作为主结论；确认无效才撤下主表并重训，保留历史记录。

## 5. P0：真机闭环与产品保护

### 5.1 小程序改动

- 拍照入口提示“从正上方拍摄整盘食物，保持清晰、光线均匀”。该提示是降低使用风险，不等于已有可靠角度检测功能。
- 结果明确为估算；不承诺斜拍/侧拍精度。
- 负值、非有限值、缺失值分别处理：异常/未知不显示成 0，不默认为有效结果，不计入合计或饮食计划。
- 五项目标各自判断有效性，不以蛋白质存在来推断其他营养字段有效。
- 保留人工修正，但用户确认、用户估计、秤测真值必须区分，不能自动进入高可信训练标签。
- 回归与分类使用不同模型时，接口分别记录两套版本、权重 SHA 和类别 schema。
- 后端校验修正类别白名单并保存标签版本；旧记录不因索引变化而被重解释。

### 5.2 真机测试

先测试功能，不把功能通过写成营养精度通过。至少检查：

- [ ] 同图离线、HTTP、小程序输出在规定容差内一致，取整规则一致。
- [ ] 拍照、相册、上传、识别、确认、保存、历史再次打开正常。
- [ ] 网络中断、超时、鉴权失败、超大图片有明确提示且不产生伪成功记录。
- [ ] 双击/重试不会产生重复记录；用户间记录隔离。
- [ ] 五项有效、部分缺失、负值、旧 11 类记录、新 12 类记录显示正确。
- [ ] 小屏、大字体、选项文字居中、标签换行与底部安全区正常。
- [ ] 使用授权与训练数据授权独立，可拒绝训练用途而继续正常使用。

由用户配合微信开发者工具登录、体验成员配置及手机操作；记录机型、微信版本、小程序版本、后端版本、图片编号、时间、预期、实际与截图。公网内测前另行核实 HTTPS/云托管调用方式、域名配置和服务鉴权，不能只用局域网浏览器 `/health` 成功代替小程序上传验收。

验收：关键流程无阻断错误，无跨用户数据泄漏，无异常值被当成正常营养值；未完成时仅允许开发测试，不宣称已适合真实用户。

## 6. P1：建立独立多角度评测集

### 6.1 优先补什么数据

先建立小规模诊断集，例如 30–50 份独立餐食；此数量只是发现问题的起点，不足以支持普遍精度承诺。

每份餐食在内容和份量不变的情况下采集俯拍、约 45° 斜拍、侧拍，可另采不同光线/距离。记录餐盘 ID、采集序列、拍摄者、设备、视角、日期、食物、熟重及测量方式。不要把手机整图数量当成独立餐盘数量。

优先覆盖小份餐、混合菜、水果、谷物、海鲜，以及普通用户常吃的中式菜。汤、甜品、酱汁等样本不足的类别先标为未验收，再定向补充。

重量可用去皮电子秤获得。营养标签若由配方、食材重量与营养表推导，应保存来源和计算过程；无法确定的油量、食材或烹饪信息保留未知，不伪造真值。

### 6.2 划分规则

- 原有 128 餐盘继续只用于训练，不将其相邻视频帧重新命名为测试集。
- 同一物理餐盘/连续加菜序列的所有图片必须在同一划分；必要时按拍摄者、餐厅或会话进一步分组。
- 新建开发验证集和封存测试集。若某批新图片用于诊断和选择方案，该批就属于开发数据，不能再宣称是最终盲测。
- 去重检查结合来源信息、图像哈希和视觉近似，不只查文件名。

### 6.3 评估方式

同一检查点在同一批餐盘的不同视角上分别报告：五目标 MAE/RMSE、热量 MAPE（说明零值处理）、MAE/均值、分类 Macro-F1、各类支持度及异常率。

额外报告同餐盘跨角度的预测差异，但低差异不能代替准确性：一个模型始终输出常数也可能非常“一致”。重复帧先按餐盘聚合，置信区间按餐盘或采集组重采样，不能把同盘多张照片当独立样本扩大样本量。

验收：能分开回答“真实误差多大”和“同盘不同角度波动多大”；不再用训练餐盘的近邻帧证明泛化。

## 7. P1：下一轮训练最小实验矩阵

先固定修正后的标签体系、五目标 RGB 基线和同一数据划分，使用 seed 42/43/44 独立 tag。每项改进只改一个主要因素，验证集选模，方案冻结后统一测试。

| 实验 | 相对对照只改变什么 | 主要验收内容 |
|---|---|---|
| A：修正版 RGB 基线 | 无；作为新协议起点 | 五项目标、分类、异常率、运行成本 |
| B：非负输出 | 输出参数化 | 负预测消除且零值、小份餐和总体误差可接受 |
| C：类别平衡 | 损失权重或采样方式，先二选一 | 少数类召回与 Macro-F1；同时观察营养回归退化 |
| D：随机视角训练 | 同盘训练视角采样 | 独立多角度误差，不只看俯拍测试 |
| E：跨视角一致性 | 在匹配输入/预算的双视角对照上加一致性损失 | 准确性与稳定性同时评估，避免常数输出 |

旧 11 类与新 12 类的历史争议如需写入论文，单独补齐同数据、同代码、同 seed 的标签对照；不把旧数据模型与扩展数据模型混成一组。两个 schema 的整体 Macro-F1 不直接横比，可另建共同粗粒度映射，同时报告水果类单独表现。

### 7.1 负预测专项

先检查标签单位、训练集统计量、归一化逆变换、目标顺序与 mask。确认无数据/实现错误后，可尝试在非负物理量参数化中使用 Softplus。

不能在标准化输出上随意增加 Softplus 后继续套用有平移项的旧逆变换；应明确完整数学映射，使最终物理量满足约束。真实零值、近零值与缺失值必须区别处理。保留旧原始预测供审计，禁止仅裁零后宣称模型已修好。

### 7.2 复现与统计专项

若同 seed 重跑不一致，按顺序比较：代码/清单哈希 → 初始化参数 → 实际 batch ID 与增强 → 优化器/续跑状态 → 精度与确定性设置。只看到第一轮损失不同，不能直接断定是 CUDA。

每组报告各 seed 结果、均值和标准差；逐 seed 比较差值。样本级置信区间与跨训练 seed 波动分开呈现。三个 seed 是最低起点，不是显著性保证，也不设未经充分测量的固定“±1 kcal”门槛。

## 8. P2：再决定 NIR、CLIP 与重量×密度

- NIR 门控：与直接拼接及 RGB 对照比较。预测 NIR 是由 RGB 派生的表征，不是新增实测光谱信息。
- CLIP 辅助：确认通用权重加载及类别文本映射；比较启用/关闭，不把模型生成的类别当成真值。
- 重量×密度：同时监督可靠重量与热量，避免仅靠乘积约束导致两个因子互相补偿；缺少可靠密度/烹饪标签时明确监督边界。
- 多视角一致性：先确保训练加载器实际读取不同视角，且不是只读 manifest 中单张 `image`。
- 各单项通过后才组合，组合收益另验；没有稳定增益或推理成本明显增加的模块不默认上线。

## 9. 外部事项解除方式

| 事项 | 需要的配合 | 不应阻塞的工作 |
|---|---|---|
| 真机/微信权限 | 用户操作手机、扫码登录、配置体验成员 | 后端检查、单元测试、接口契约与测试清单 |
| 论文口径 | 补充可访问全文或作者版本，逐表核对 | 当前模型工程整改 |
| 全量数据 | 明确许可、磁盘容量、下载预算和来源后再启动 | 定向小批数据与独立评测集建设 |
| 队友权重 | 权重+代码版本+预处理+标签映射+训练划分+评估材料+SHA | 当前模型闭环与独立验证 |
| 队友未提交代码 | 保存现场，确认归属与冲突，逐项合并 | 无冲突区域的工作 |
| GitHub 凭据 | 最终交付时在本机完成授权，不在聊天发送 Token | 本地开发与实验 |

全量 HSI 与 Nutrition5k 用于不同任务，不能把“下载到 3,389 对 HSI”当作与主参考论文实验完全对齐。更大的数据集也不能代替采集协议、标签与划分的核对。

## 10. 交付物与停止条件

建议形成以下独立产物，文件名可按仓库约定调整：

- 实验登记表：每次运行的代码、数据、权重和标签版本。
- CLIP 加载核验报告：保留/待核实/撤下及原因。
- 受影响运行清单：选模缺陷范围与重训/重评决定。
- 真机验收记录：逐项结果、机型、版本和截图。
- 独立多角度数据说明与划分清单。
- 配对多种子结果表：原始预测、指标、异常率、限制。
- 发布/回退说明：线上分类、回归分别使用什么模型。

遇到跨划分泄漏、权重加载失败、标签单位错误、非有限预测或用户数据隔离失败，应暂停该候选的发布与正式结论。训练依然未完成、测试仍未执行的项目，必须保留“待验证”，不能用 smoke 成功代替验收。

近期完成标准：实验可追溯、真机流程可用、独立视角评测成立。达到后开展小规模自愿内测；不宣称医疗诊断、减重疗效或所有拍摄场景均准确。

## 11. 依据与阅读入口

本地核对材料（原文中部分结论需按本方案修正）：

- `yitu-zhika-code/docs/侧视检验结果_2026-09-10.md`
- `yitu-zhika-code/docs/阶段四_扩展训练与可靠性核对_2026-09-10.md`
- `yitu-zhika-code/docs/在线模型切换核对表_2026-09-10.md`
- `yitu-zhika-code/src/training/train_calorieclip_official.py`
- `yitu-zhika-code/results/calorieclip_official_v1/cpu_validation.json`

公开依据：

- [PyTorch：Reproducibility](https://docs.pytorch.org/docs/2.14/notes/randomness.html)：固定 seed 不等于在所有环境中逐位可复现；按本项目安装版本核对可用设置，不为套用文档而升级运行环境。
- [PyTorch：Softplus](https://docs.pytorch.org/docs/2.14/generated/torch.nn.Softplus.html)：非负物理量参数化的候选工具，不是提高精度的保证。
- [Nutrition5k 原论文](https://arxiv.org/abs/2103.03375)：数据、重量与营养预测的参考。
- [Multi-Spectral Food Classification and Caloric Estimation Using Predicted Images](https://pmc.ncbi.nlm.nih.gov/articles/PMC10887625/)：预测多波长路线的参考；98.24%/12.13% 不可直接作为本项目同口径对照，具体表格条件仍需逐项核实。
- [CLIP 原论文](https://proceedings.mlr.press/v139/radford21a.html)：图文表征与迁移依据，不提供本项目克重或油量准确性的证明。
- [CalorieCLIP 模型项目](https://huggingface.co/jc-builds/CalorieCLIP)：公开软件基线，不能当作已核实的同行评审论文。

执行边界：本文件不授权覆盖队友改动、删除历史产物、购买云资源、发布小程序、上传用户照片或自动推送 GitHub；需要这些动作时另行确认具体范围。

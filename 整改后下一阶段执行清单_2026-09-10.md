# 一图知卡：整改后下一阶段执行清单

日期：2026-09-10
工作台：`C:/Users/user/Desktop/新建文件夹`
目标：验收五目标模型，确认标签纠偏进入训练，再接入微信小程序。

## 1. 当前已核查的进展

| 项目 | 已核查状态 | 不应据此推断 |
|---|---|---|
| 标签规则和原消融训练续跑测试 | 6项测试通过 | 不代表新五目标训练脚本已支持可靠恢复 |
| 五目标模型 v1 | 状态文件记录完成30轮，最佳轮次22；权重SHA与指标文件匹配 | 不代表指标统计、选模及真机表现均已验收 |
| 测试预测 | 507行、507个不同餐盘 | 不代表报告中的全部汇总统计正确 |
| 新增128餐盘实验 v1_expanded | 最近检查的状态文件记录第2轮，未见完成报告 | 不代表进程目前仍在运行 |
| 小程序营养素展示 | 已有显示字段与不支持提示 | 不代表后端已加载五目标权重 |
| 当前主推理 | 仍设置宏量营养unsupported，并返回空营养素字段 | 新权重存在不会自动生效 |

本清单基于上一轮只读检查结果。若队友已继续执行，以新的状态、权重及验证证据更新结论；不要仅凭文档中的完成标记判断。

## 2. 执行顺序

**评估审计 → 标签版本核对 → 五目标接口接入 → 真机验收 → 扩展数据对照 → 新模块消融。**

先保护现有产物，不覆盖原模型，不按测试集成绩反复选型，不在验收前同时启动多个新方案。

## 3. 第一阶段：验收五目标训练结果（最高优先级）

涉及文件：

- [训练脚本](C:/Users/user/Desktop/新建文件夹/yitu-zhika-code/src/training/train_meal_macros.py)
- [五目标模型](C:/Users/user/Desktop/新建文件夹/yitu-zhika-code/src/models/meal_macros_net.py)
- [训练状态](C:/Users/user/Desktop/新建文件夹/yitu-zhika-code/results/meal_macros_train_v1/v1/status.json)
- [测试指标](C:/Users/user/Desktop/新建文件夹/yitu-zhika-code/results/meal_macros_train_v1/v1/test_metrics.json)
- [逐餐盘预测](C:/Users/user/Desktop/新建文件夹/yitu-zhika-code/results/meal_macros_train_v1/v1/test_predictions.csv)
- [当前五目标权重](C:/Users/user/Desktop/新建文件夹/yitu-zhika-code/checkpoints/meal_macros_v1/v1/best.pt)

### 已发现的问题

报告总样本数为16136，但逐目标有效样本数均为507，预测CSV也是507个不同餐盘。原因尚未定位，不能只把数字改成507就算修复。

现有报告还记录负预测：热量4、重量3、蛋白质19、碳水3、脂肪25。需从原始预测复核并制定产品异常处理，不把它们作为正常营养值显示。

### 执行任务

- [ ] 保留原始status、epochs、预测CSV和权重，记录SHA及检查时间。
- [ ] 从CSV独立重算五项MAE、RMSE、有效样本数和负预测数量；未知标签与真实0分开。
- [ ] 检查各批次样本计数、损失累加和分母，定位16136的来源。
- [ ] 检查同类统计问题是否影响训练/验证日志和最佳轮次选择。
- [ ] 若仅报告字段有误，生成新版本审计及更正说明，不无声覆盖原证据。
- [ ] 若验证选模受影响，只用可获得且来源明确的候选权重重新验证；无法恢复候选时说明限制，再安排受控重训，不用测试集挑选。
- [ ] 核对目标顺序、单位、反归一化、manifest哈希和分类映射与权重一致。
- [ ] 保留原始负预测用于评估；产品端返回待确认/估算异常，不能通过事后裁零美化指标。

### 验收标准

- 报告中的样本数与逐餐盘证据一致；缺失目标使用各自有效分母。
- 五项目标指标可独立复算，差异在说明的数值容差内。
- 最佳模型由正确的验证规则选出，版本与权重SHA对应。
- 形成明确的“通过/带限制通过/不通过”结论，未通过不切换主服务。

## 4. 第二阶段：确认标签修正实际进入数据与训练

涉及文件：

- [类别规则](C:/Users/user/Desktop/新建文件夹/yitu-zhika-code/src/data/build_categories.py)
- [类别规则测试](C:/Users/user/Desktop/新建文件夹/yitu-zhika-code/tests/test_build_categories.py)
- [五目标清单构建](C:/Users/user/Desktop/新建文件夹/yitu-zhika-code/src/data/meal_macros_manifest.py)

关键区别：**规则修复 ≠ 已有训练标签更新 ≠ 已训练权重吸收修复。**

当前五目标清单构建代码仍从旧冻结清单复制category/category_idx。需要核对实际新旧标签，不可直接宣称“蔬菜偏置已经解决”。

- [ ] 对真实食材表运行修正规则的只读比较，输出食材、旧类、新类、命中原因及受影响餐盘。
- [ ] 优先人工核查旧规则误匹配、少数类别、高分错例和混合餐食。
- [ ] 明确水果/蔬菜、菜名/主要食材组的语义，类别集合变化时升级label_schema。
- [ ] 生成新版本清单，记录标签来源、规则版本和SHA，不覆盖旧冻结manifest。
- [ ] 同一餐盘和多视图不跨集合；对修订测试标签保留新旧版本差异，不能混用口径。
- [ ] 核对训练/验证各类别支持数；没有验证样本的类别不得宣称完成该类验收。
- [ ] 下一轮训练的配置、数据加载和checkpoint均记录实际新manifest哈希。

验收：从某个被修正餐盘出发，能追踪“修正规则→新标签→新manifest→训练配置→权重”；旧模型仍保留作对照。

## 5. 第三阶段：接入真正的五目标推理

前置条件：第三节评估审计通过；若分类仍受限，接口和界面必须如实说明，不把待整改能力宣称为已解决。

涉及文件：

- [主推理管线](C:/Users/user/Desktop/新建文件夹/yitu-zhika-code/app/experiment_pipeline.py)
- [服务入口](C:/Users/user/Desktop/新建文件夹/yitu-zhika-code/app/inference_service.py)
- [接口契约](C:/Users/user/Desktop/新建文件夹/yitu-zhika-code/app/contracts.py)
- [小程序结果页](C:/Users/user/Desktop/新建文件夹/miniprogram/pages/result/result.wxml)

- [ ] 新增或完成MealMacrosNet的严格推理适配，使用准确的五目标manifest及权重。
- [ ] 不只修改ONLINE_MODEL_PATH；确认服务真正调用了所选模型对象。
- [ ] model_version、SHA、target_names、label_schema和预处理来自同一已验证模型描述。
- [ ] 正确映射protein_g、carbohydrate_g、fat_g，单位为g，不按列位置猜测。
- [ ] 完成验证后才设置macros_status为supported；加载失败不可假装成功。
- [ ] 对五字段分别处理缺失/异常，不因蛋白质有值就假设另外两项也有效。
- [ ] 兼容只有热量和重量的旧历史记录；缺失显示待补充，真实0显示0。
- [ ] 保留旧服务版本与回退方式；不要覆盖原权重。

验收：同一固定图片在离线模型、HTTP接口和小程序中数值与单位一致；返回版本匹配真实权重；异常、缺失及旧记录均不会显示伪造数值。

## 6. 第四阶段：确认扩展训练与后续训练可靠性

### 6.1 新增128餐盘实验

最近检查的v1_expanded只有第2轮状态，没有最终报告。先核对进程及日志，不因权重文件存在就认定训练完成。

- [ ] 检查任务仍在运行、已退出还是暂停；保存状态和退出原因。
- [ ] 核对实际读取扩展manifest，而非只建立同名输出目录。
- [ ] 验证新增128餐盘仅进入训练，原验证/测试划分不泄漏。
- [ ] 验证加载器正确消费新增视图。当前五目标加载器继承俯拍MealDataset，不能仅因manifest包含views就认定已实现多视角训练。
- [ ] 完成后先按第三节的规则审计，再和旧数据五目标模型比较。

### 6.2 不覆盖的多种子运行和续跑

现有P1-B说明把不同seed都放在同一--tag v1下，存在覆盖风险。以后用独立tag，例如v2_seed42、v2_seed43、v2_seed44；这些只是命名建议，不表示已启动实验。

新五目标脚本与原消融脚本不是同一条续跑链路。原test_ablation_resume通过，不等于train_meal_macros已支持恢复。

- [ ] 启动前检查输出目录；非空目录必须明确拒绝新跑或走经过验证的resume。
- [ ] checkpoint记录optimizer、随机状态、epoch、history、数据/代码哈希及完整配置。
- [ ] 明确原子保存与中断恢复；不能重新运行同一命令就当作自动续训。
- [ ] 检查初始化种子在模型创建前生效，以及采样种子使用预期seed，保证种子实验可复现。
- [ ] 修正文档，避免发出会覆盖既有v1的命令。

验收：小预算训练中断后可正确恢复，已有模型不被覆盖，不同种子有独立且完整的产物。

## 7. 第五阶段：微信小程序真机内测

先在同一局域网做功能验收，通过后再考虑云托管和异地群体测试。

- [ ] 拍照/相册→上传→五目标返回→修改/确认→历史回看全流程完成。
- [ ] 用不同类别及混合餐食检查输出，保留文件与预测对应关系；文件名不能代替人工真值。
- [ ] 称重样例用于重量误差检查；没有可靠营养标签的照片只检查交互和分类，不计算营养精度。
- [ ] 检查网络失败、鉴权失败、模型不支持、负值/异常值和旧记录。
- [ ] 问卷按钮、足迹卡片、饮食计划表单在小屏及大字体设置下不溢出。
- [ ] 用户估计、确认和实测分别标记；用于训练的同意独立于普通记录功能。
- [ ] 小范围测试只用于可用性及缺陷发现，不把少量成功案例宣传为泛化准确率。

通过后再进入NIR门控、CLIP语义辅助、重量×密度、多视角一致性的逐项消融。先固定对照、种子预算和验收口径，不一次叠加全部模块。

## 8. 当前可以安全执行的核查命令

以下命令不启动训练、不替换权重、不发布服务。

### 8.1 回归测试

```powershell
Set-Location 'C:\Users\user\Desktop\新建文件夹\yitu-zhika-code'
& 'C:\Users\user\miniconda3\envs\yitu\python.exe' -m unittest tests.test_ablation_resume tests.test_build_categories -v
if ($LASTEXITCODE -ne 0) { throw '回归测试未通过，先处理失败项' }
```

上次检查结果：6项通过。它们不覆盖全部五目标训练与接口行为。

### 8.2 查看扩展实验状态

```powershell
Get-Content -LiteralPath 'C:\Users\user\Desktop\新建文件夹\yitu-zhika-code\results\meal_macros_train_v1\v1_expanded\status.json'
```

状态文件可过期；应结合进程存活、日志时间及最终产物确认。

### 8.3 检查预测行数

```powershell
$taskCsv = 'C:\Users\user\Desktop\新建文件夹\yitu-zhika-code\results\meal_macros_train_v1\v1\test_predictions.csv'
$taskRows = @(Import-Csv -LiteralPath $taskCsv)
'预测行数：' + $taskRows.Count
'不同餐盘数：' + @($taskRows.dish_id | Sort-Object -Unique).Count
```

上次两项均为507。这里只检查数量，不替代完整指标重算。

### 8.4 UI基础测试

```powershell
node --test 'C:\Users\user\Desktop\新建文件夹\miniprogram\tests\ui-regression.test.cjs'
```

## 9. 阶段交付物

1. 五目标评估审计：原始/重算指标、样本数问题定位、选模影响、通过结论。
2. 标签版本差异：实际受影响食材/餐盘、审核结果、新manifest哈希。
3. 五目标接口验收：离线/HTTP/小程序同图对照、异常值与旧记录测试。
4. 扩展数据实验审计：实际清单、完成状态、固定口径对照。
5. 真机内测记录：设备、版本、操作、预期、实测、缺陷优先级。

本次仅新增执行文档；未启动训练、未替换线上模型、未修改队友代码。项目较完整后再按用户要求整理推送GitHub，最终交付保持微信小程序形态。


## Agent skills

### Issue tracker

使用 GitHub Issues；操作 issue/spec 前读取 `docs/agents/issue-tracker.md`。

### Triage labels

使用五个默认状态标签；执行 triage 时读取 `docs/agents/triage-labels.md`。

### Domain docs

采用 single-context；探索工程代码前读取 `docs/agents/domain.md`。

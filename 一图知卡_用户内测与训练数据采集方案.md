# 一图知卡：用户内测与训练数据采集方案

日期：2026-09-08  
文档性质：待实施方案，不代表功能、用户招募或数据采集已经完成。  
最终产品：微信小程序。Gradio Demo 仅用于模型展示、训练测评与实验对比。

## 一、目标与边界

面向有体重管理需求的自愿成年用户开展小范围内测，收集真实手机拍摄的餐食、用户修正和可靠标注，改善模型对中餐、外卖、混合餐及不同拍摄条件的适应能力。

第一阶段验证的是产品可用性、数据采集流程和标签质量，不验证减肥效果，不承诺热量估计准确率或减重结果。用户不应仅凭模型估算调整节食强度；不以食物摄入越少、体重下降越快作为奖励依据。

核心原则：更多照片不等于更多可靠标签。模型预测、用户估计、实测重量和经过核验的营养参考值必须分开保存。

## 二、首批内测安排

- 建议先邀请 10–20 名自愿成年用户，开展约 7 天的流程试用；这是产品试用规模建议，不是证明模型有效的统计样本量。
- 优先覆盖家庭餐、食堂餐、外卖、单一食物和混合餐，而不是只收集容易识别的轻食照片。
- 不要求参与者为了采集数据改变正常饮食。
- 未成年人、孕哺期人群、进食障碍相关人群或需要疾病饮食管理的人群，不纳入首版自动饮食建议试用范围；特殊情况由合适的专业人员处理。
- 不在群内公开个人体重、饮食记录、健康情况或参与排名。
- 先提供说明、退出和反馈渠道，再邀请用户开始使用。

## 三、小程序的两条采集流程

### 1. 普通记录：优先保证易用性

拍照或选图 → 查看模型估算 → 自愿修正 → 确认保存。

记录包含餐食照片、时间、模型版本、原始预测及用户确认后的内容。界面明确显示哪些是模型估算、哪些是用户填写，不自动将估算当作真实摄入。

正常使用不以同意训练用途为前提。不同意用于训练的记录，不进入训练数据池。

### 2. 自愿标注：提高数据可信度

普通记录完成后，提供独立、可跳过的入口：

> 愿意帮助改善识别吗？你可以补充菜名、称重结果或另一角度照片。是否参与不影响正常使用。

用户可选择补充菜名、主要食材、重量、烹饪方式和另一角度照片。不知道的项目允许留空或选择“不知道”，不能强制猜测。

已同意训练用途并通过质量审核的数据，才进入相应训练候选池。

## 四、采集字段与用途

| 数据 | 建议采集方式 | 可用范围与限制 |
|---|---|---|
| 餐食照片 | 自愿上传，尽量清晰、包含完整餐盘 | 识别训练候选；检查背景隐私、重复图片和图像质量 |
| 菜名、主要食材 | 用户确认或修正，可选多个 | 类别或食材标签候选；混合餐不能强行标为单一食物 |
| 食物重量 | 电子秤称重，记录单位、是否去皮/去餐具、生熟状态和测量时间 | 可靠重量监督；用户目测重量单独标注，不能当实测 |
| 烹饪方式 | 用户填写蒸、煮、炒、炸等，可选未知 | 用户声明的烹饪标签，经过审核后使用；不是照片测量真值 |
| 配方与配料重量 | 烹饪者自愿记录，并注明对应整锅还是当前份量 | 支持营养参考计算；处理分餐比例和剩余部分 |
| 用油信息 | 区分整锅添加油、剩余油和分餐信息，允许未知 | 添加油不等于该份食物实际留存油，不直接生成留存油真值 |
| 营养参考值 | 食品标签或有来源的食材营养数据结合配方计算 | 标注为参考值及其来源，不称为实验室测得的真实热量 |
| 第二角度照片 | 同一餐盘、同一次进食前拍摄 | 多视角一致性训练；进食后份量变化的照片不能当同一目标 |
| 识别反馈 | 类别不对、份量偏大/偏小、识别失败、其他 | 用于问题分析和审核优先级，不直接转换成定量真值 |

不收集与本轮模型改进无关的身份信息。体重、身高和病史不是食物识别训练的必需标签；不得以“优化模型”为由默认收集。

## 五、最小数据结构与溯源

建议至少记录以下字段，具体名称可随现有代码规范调整：

| 字段 | 含义 |
|---|---|
| record_id | 餐食记录唯一编号 |
| participant_id | 去标识化参与者编号，与身份映射分离保存；不是匿名化保证 |
| meal_id / capture_session_id | 同一餐盘及拍摄会话，关联多张照片 |
| image_id / image_hash | 图片编号与去重校验值 |
| model_version | 产生原始预测的模型版本 |
| original_prediction | 原始模型输出，保留用于分析，不作真实标签 |
| label_value / label_source | 标签值与来源，例如用户估计、秤重、包装标签、配方参考计算 |
| mass_basis / tare_status | 生重、熟重、供食重量、未知，以及是否去除餐具重量 |
| cooking_method / oil_information | 用户声明及未知状态，不能用默认值补齐 |
| review_status / review_reason | 待审核、通过、需补充、排除及原因 |
| training_consent_version / consent_time | 训练用途授权版本和时间 |
| dataset_version / split | 实际进入的数据版本及训练、验证、测试归属 |

原始预测与后续标签分别保存，避免用户修正覆盖后无法追溯。未知值使用空值并标注原因，不能用 0 表示缺失热量或缺失重量。

## 六、标签质量分层

- **已核验标签**：测量方法或营养来源明确，并通过人工或规则核验。仅对核验通过的字段进行对应监督训练。
- **用户声明标签**：菜名、烹饪方式或估计份量等，来源明确但未核验。可进入审核队列或单独的弱监督实验。
- **仅图像/反馈数据**：没有可靠数值标签。用于失败分析、场景统计或另立的无标签实验，不伪造热量和重量。

分层应按字段进行：一条记录可能有可靠重量，但烹饪方式、油量和热量仍未知。“整条记录通过”不意味着所有字段都可靠。

禁止把模型预测的热量直接回填为监督真值；也不能把用户随手填写的热量与有称重、配方依据的参考值同等对待。

## 七、用户授权与隐私

1. 区分提供记录服务所需的数据处理与自愿用于模型训练的授权，训练选项默认不勾选。
2. 说明采集内容、用途、保存期限、访问人员、第三方服务、撤回方式和联系方式；保存期限应在招募前明确。
3. 允许拒绝参与或退出，提供删除申请入口；说明删除原始数据、从后续数据版本排除和已训练模型处理之间的区别，不承诺无法兑现的即时模型遗忘。
4. 默认只保留完成任务所需的图片内容；去除不必要的 EXIF 定位信息，提示避开人脸、订单姓名、地址和电话号码。
5. 图片私有存储，访问控制和传输保护到位；不把原图、身份映射、健康记录或密钥推送到 GitHub。
6. 不默认将用户照片、身份信息或完整健康档案发送给大模型供应商。确需第三方处理时，事先说明并落实适用的授权和保护要求。
7. 处理涉及医疗健康的敏感个人信息时，核对必要性、单独同意及相应保护措施。上线前还需完成微信平台适用的隐私声明和发布要求。

法律参考：[《中华人民共和国个人信息保护法》](https://www.npc.gov.cn/npc/c2/c30834/202108/t20210820_313088.html)。本方案是产品实施草案，不替代具体上线场景的合规审查。

## 八、审核与训练流程

用户授权 → 数据采集 → 图像质量/重复/隐私检查 → 标签审核 → 冻结数据版本与划分 → 离线训练 → 独立评测 → 人工决定是否发布。

### 1. 审核要求

- 检查单位、餐具重量、照片与测量是否对应同一份食物。
- 检查菜名、配方、重量和营养来源是否相互一致。
- 极端值先核查，不仅凭热量高低删除；低热量食物不自动判错。
- 记录排除原因，保留审核日志；仅向有权限人员开放原始资料。

### 2. 防止评测泄漏

- 同一餐盘的不同角度、重复图片必须在同一划分内。
- 对“泛化到新用户”的评测，按参与者分组划分，不能把同一用户的数据随机散入训练和测试。
- 其他评测如跨时间、跨场景，应预先定义协议并单独报告，不混淆为新用户泛化。
- 不同分量或进食阶段的照片可以关联，但不能强行施加相同重量、热量目标。
- 保留不参与训练的真实场景测试数据；样本不足时明确说明，不能靠同餐盘多帧夸大独立样本量。
- 验证集用于选模和参数选择，测试集不用于反复调参；持续迭代时另行补充保留集，控制重复使用测试集造成的偏差。

### 3. 训练与发布

- 模型改进分别保留对照：数据扩容、NIR门控、重量×密度、多视角一致性、CLIP辅助不得混在一起后宣称每项都有效。
- CLIP可提供语义提示或审核线索，不充当重量、留存油量及烹饪方式真值。
- 对外展示模型仍使用经过验收的固定版本；新数据不触发未经审核的自动上线。
- 记录代码、清单、标签、权重和结果的版本；新模型通过质量与稳定性检查后再灰度替换，并保留回退能力。

## 九、食谱功能在内测中的边界

- 首版优先使用核验过的模板和可追溯营养数据，允许用户替换和调整。
- 不把模型推测的摄入量直接用于自动安排补偿性节食或运动。
- 缺少营养数据、过敏原或可靠生熟换算时，提示待确认，不能显示虚假的合计或精度。
- 区分AI建议草案与真人专业人员审核的方案；没有审核记录不得显示“已由营养师审核”。
- 收集食谱是否易理解、方便执行的反馈，不将短期体重变化当作食谱效果的因果证据。

## 十、首批验收与指标

### 上线内测前

- [ ] 微信小程序可真机完成拍照、修正、保存和反馈。
- [ ] 训练授权可以单独拒绝，不影响正常记录。
- [ ] 同一餐盘的多角度照片能正确关联。
- [ ] 模型预测、用户估计和实测标签可区分、可追溯。
- [ ] 上传限制、鉴权、限流、私有存储和错误恢复通过检查。
- [ ] 删除/退出流程和数据保存期限已明确。
- [ ] 未经验证的精度、误差区间和减肥效果声明已移除。
- [ ] 食谱的未知信息、忌口校验和审核状态不会误导用户。

### 试用后报告

- 流程：记录成功率、完成时间、失败类型、用户修正率、退出原因。
- 数据：独立参与者/餐盘数、场景分布、重复率、各字段可用率、实测标签比例。
- 模型：固定评测口径下的类别表现、重量/热量MAE与RMSE；MAPE单独说明零值处理，低热量样本如实报告。
- 体验：清晰度、掌控感、信任度，以及是否感到被催促或被评判。
- 安全：授权问题、隐私事件、过敏/食谱问题及处理情况。

首批人数和使用天数不构成模型精度或减肥有效性的充分证据。是否扩大测试，依据问题是否解决、标签质量与独立评测情况决定。

## 十一、实施优先级与职责

1. 先完成小程序记录、修正、自愿标注和反馈流程。
2. 同步补齐服务端授权记录、私有存储、审核状态与版本溯源。
3. 由项目负责人确认招募说明和数据处理规则，指定审核及问题响应人员。
4. 试用前做完整演练，确认没有把未知值当零或把预测当真值。
5. 小批量采集、审核并冻结首个用户数据版本，再开展离线对照训练。
6. 项目大体完成并验收后再推送GitHub；仅推送代码、可公开文档与去敏汇总，不推送用户原始数据和密钥。

成功标准不是“收集了很多照片”，而是形成一条用户自愿参与、标签可靠、可以审计、能够验证改进的数据闭环。

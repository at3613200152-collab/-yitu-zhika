# 阶段四核对：扩展训练与训练可靠性（整改清单 §6）

日期：2026-09-10　对象：`v1_expanded`（+128=3390 五目标）+ 训练脚本可靠性与多种子。

## 1. §6.1 新增 128 餐盘实验核对

### 1.1 训练确实完成（不是"有权重就算完成"）
`results/meal_macros_train_v1/v1_expanded/status.json`：

| 字段 | 值 |
|---|---|
| stage | `complete` |
| epochs_planned / best_epoch | 30 / 29 |
| val reg_normalized_l1（best） | 0.3798 |
| val kcal MAE | 73.15 |
| test n | 507 |
| 产物 | `test_metrics.json` / `test_predictions.csv` / `epochs.json`(30) 齐全 |

### 1.2 确实读了扩展 manifest（而非只建了同名目录）
扩展 manifest 的 `new_dishes_from_expanded=128`；两次训练的**实际训练样本数**（取自 `epochs.json` 的 `n`）：

| 运行 | train n | val n | test n |
|---|---|---|---|
| `v1`（冻结 3262） | 2188 | 567 | 507 |
| `v1_expanded`（3390） | **2316** | 567 | 507 |
| 差值 | **+128** | 0 | 0 |

即新增 128 餐盘**只进入训练**，验证/测试划分与冻结协议完全一致。

### 1.3 无划分泄漏（`scripts/audit_expanded_manifest.py`）
- 同一 split 内无重复 `dish_id`；`train∩val`、`train∩test`、`val∩test` 均为空；
- 3390 行中**没有跨 split 相同 `image_sha256`/`pixel_sha256`**（无同图泄漏）；
- `base_ids_missing_from_expanded = []`（扩展是冻结清单的超集，没有丢盘）；
- 128 个新盘按 split 统计：train 128 / val 0 / test 0。

### 1.4 加载器的诚实说明（不能宣称"已实现多视角"）
扩展 manifest 的行**没有 `views` 字段**，`MacrosDataset` 继承俯拍 `MealDataset`，只读单张 `image`
（`size`、`pixel_sha256` 均为该单图）。因此：**当前不存在多视角训练**，只有单张俯拍视图；
"多视角一致性"属于未实现的待办，不得在报告中写成已完成。

### 1.5 发现的溯源缺口（已在新代码中补上）
`v1_expanded` 的 `best.pt/last.pt` 的 `config` **没有记录 `manifest_sha256` 与 `code_sha256`**
（只有 tag/seed/epochs/batch/protocol），所以"它确实用了扩展清单"当时**无法从权重自证**，
本次是用训练样本数（2316 vs 2188）反推证明的。
新代码起，`config` 记录：`manifest`（路径）+ `manifest_sha256` + `code_sha256` +
`num_classes` + `label_schema_version` + `sampler_seed_base`/`sampler_rule`。

## 2. §6.2 不覆盖的多种子运行与续跑

### 2.1 实测结果（`train_meal_macros.py`）

| 场景 | 命令要点 | 结果 |
|---|---|---|
| 防覆盖 | 对已有非空 tag 直接重跑 | **exit 1**，`[拒绝覆盖] …请使用新的 --tag，或确认要续跑时显式加 --resume` |
| 正常完整跑 | `--tag resumechk --epochs 2 --limit 2` | exit 0，产出 best/last/epochs/test 齐全 |
| 显式续跑 | 同 tag 加 `--resume --epochs 4` | `{"stage":"resumed","next_epoch":3}` → 续跑 epoch 3–4 → exit 0 |
| 中断恢复 | 后台跑到 epoch 3 时**强杀进程** | 目录无 `.tmp` 残留（原子写生效）；`--resume` 从 epoch 4 继续到 6 并完成，exit 0 |
| 数据变化续跑 | `--resume` 但换 manifest | **exit 1**，`[--resume] manifest 已变化，拒绝在旧产物上续跑` |

说明：`best.pt` 只存 `model/epoch/best/best_epoch/config`（保证 `weights_only=True` 可加载，推理/评估侧只读它）；
续跑状态（`optimizer/history/rng`，含 numpy 随机状态）单独存 `last.pt`，`--resume` 显式
`weights_only=False` 读取本地受信文件。开发中一度把 rng 写进 `best.pt`，导致收尾评估
`torch.load(..., weights_only=True)` 抛 `UnpicklingError`，已修复（老产物不受影响）。

### 2.2 修复：采样种子没有使用 `--seed`（真实缺陷）
- 修复前：`DataLoader(generator=torch.Generator().manual_seed(42 + epoch))` —— **硬编码 42**，
  `--seed` 只影响权重初始化与 `seed_epoch` 的全局种子，**多种子的采样顺序完全相同**，种子实验不独立。
- 修复后：`manual_seed(args.seed + epoch)`（与官方脚本 `BASE_SEED+epoch` 口径一致）。
- 兼容性：seed 42 下 `args.seed+epoch == 42+epoch`，与历史 `v1_expanded` **数值行为一致**，
  因此可用 `v2_seed42` 直接做复现性对照；seed 43/44 才是真正独立的采样。
- 初始化种子确实在模型创建前生效：`set_base_seed(args.seed)` 在 `MealMacrosNet(...)` 之前调用。

### 2.3 文档修正
`docs/P1B_五头训练运行说明_2026-09-09.md` 已把"多种子共用 `--tag v1`"改为独立 tag
（`v2_seed42/43/44`），并写明"重复原命令不会自动续训，会以拒绝覆盖退出"。

## 3. 多种子运行与"运行间噪声"实测（关键）

### 3.1 同种子同配置重跑对照（`v1_expanded` → `v2_seed42`）
两者配置完全一致（扩展清单 3390、seed 42、batch 8、30 轮、早停 8、验证归一化 L1 选模），
且修复后的采样种子在 seed 42 下与历史硬编码值等价（`42+epoch`），**理论上应逐位复现**。实测：

| 指标 | v1_expanded | v2_seed42 | 差 |
|---|---|---|---|
| best_epoch | 29 | 29 | 0 |
| test calories MAE | 58.505 | 57.520 | **−0.985** |
| test mass MAE | 36.137 | 36.350 | +0.213 |
| test protein MAE | 5.705 | 5.517 | −0.188 |
| test carbohydrate MAE | 6.098 | 6.201 | +0.103 |
| test fat MAE | 4.324 | 4.238 | −0.085 |
| test coarse category acc | 0.7377 | 0.7258 | **−1.18 pt** |

而且**从第 1 轮验证损失就不同**（0.5963 vs 0.5861，训练样本数同为 2316），说明差异不是数据顺序造成的，
而是 **CUDA 侧数值非确定性**（bf16 autocast 的归约/卷积算法选择等）。

### 3.2 结论（必须写进报告的口径修正）
1. **单次运行的指标不可当作精确值**：同配置重跑的热量 MAE 波动可达 ~1.0 kcal（≈1.7%），
   粗分类准确率波动约 1.2 个百分点。报告里不应把 MAE 写到小数点后两位并据此宣称微小提升。
2. **此前"补数据收益"的结论需要降级**：P1-C 记录的 `v1`(58.91) → `v1_expanded`(58.50)
   差 −0.41 kcal，**小于同种子重跑噪声（~1 kcal）**，因此不能据此宣称"补 128 盘带来收益"；
   该结论需要多种子（同种子配对）比较才能判定，已按 §3.3 安排。
3. 想逐位复现需要 `torch.use_deterministic_algorithms(True)` + `cudnn.deterministic=True`
   （当前运行未启用；若要启用应作为显式开关并记录在配置里，不能默默改变历史运行的可比性）。

### 3.3 正在跑的多种子
| tag | seed | 清单 | 状态 |
|---|---|---|---|
| `v2_seed42` | 42 | 扩展（旧 11 类标签） | ✅ 完成（用于上表的复现性对照） |
| `v2_seed43` / `v2_seed44` | 43 / 44 | 扩展（旧 11 类标签） | ⏹ 主动停止（见下） |
| `v3_corrected_seed42/43/44` | 42 / 43 / 44 | 修正（label_schema v2，12 类） | ⏳ 运行中 |

为什么停掉 `v2_seed43/44`：label_schema v2 已采纳（新增 fruit），继续在旧 11 类标签上消耗 GPU
的价值低于直接用修正清单做多种子；`v2_seed42` 已完成，其"旧标签单种子"角色已足够做复现性对照。
`v2_seed43` 在 epoch 1 内被停止，其产物**不完整、不得用于任何指标**（保留目录以便审计"确实被停止"）。

## 4. 边界
- 本节只核对与修复**训练可靠性**；分类准确率仍受 label_schema 版本影响（见
  `docs/标签版本核对_2026-09-10.md` 与 label_schema v2 记录）。
- 未做云 GPU、未做多卡；单机单卡 8h 预算保护仍在（`stage=paused_budget`）。
